# 可选：若使用 docker compose 的 .env 覆盖，可参考本文件。
# 快速安装更推荐直接修改 docker-compose.yml / docker-compose.redis.yml 中的 environment。

ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me
MOYIN_SECRET_KEY=change_me_secret
# REDIS_PASSWORD=change_me_redis   # 仅标准版；需与 compose 内 REDIS_URL 一致
# LIBRARY_HOST_PATH=/path/to/your/ebooks
